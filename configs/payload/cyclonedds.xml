<?xml version="1.0" encoding="UTF-8"?>
<!--
  CycloneDDS config for the GO2 Orin NX payload.

  Ships in the repo so nothing has to be hand-built at the lab. The interface
  name is the whole point: CycloneDDS binds to a NAMED NIC, and if that name
  does not exist on the host then every Go2 topic is advertised and carries NO
  DATA. Discovery still works, so `ros2 topic list` and `ros2 topic info` look
  completely healthy; only `hz`/`echo` reveal it. A URI inherited from a desktop
  control PC (enp0s31f6 and similar) silently produces exactly that state.

  enP8p1s0 is the robot-facing NIC on the payload (docs/go2_field_notes.md s1).
  Verify before trusting it:
      ip -brief link show enP8p1s0        # must exist and be UP

  Use as a FILE uri, never inline XML: unitree_ros2/setup.sh exports inline XML
  and the Phoenix harness halts on it rather than guess.
      export CYCLONEDDS_URI=file://$PWD/configs/payload/cyclonedds.xml
-->
<CycloneDDS xmlns="https://cdds.io/config"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain Id="any">
    <General>
      <Interfaces>
        <NetworkInterface name="enP8p1s0" priority="default" multicast="default" />
      </Interfaces>
      <AllowMulticast>true</AllowMulticast>
    </General>
  </Domain>
</CycloneDDS>
