# Phase B3 (SIMULATION REFERENCE ONLY, never a deploy option): no soft limiter.
# The hard joint limits are enforced by the simulator's joint limits.
defaults:
  - ../stand_v3_h25
action:
  rate_limit:
    enabled: false
