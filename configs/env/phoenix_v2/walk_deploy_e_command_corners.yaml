# Phase 9 condition E held-out command combinations: all three near their corners at once (amendment 9). Exact deploy stack around Isaac Lab:
# the env's own soft limiter is OFF, the deploy ActuatorGate is the only limiter.
defaults:
  - walk_w1
action:
  rate_limit:
    enabled: false
domain_randomization:
  enabled: false
command:
  lin_vel_x: [0.7, 0.9]
  lin_vel_y: [0.4, 0.5]
  ang_vel_z: [0.7, 0.9]
  rel_standing_envs: 0.0
