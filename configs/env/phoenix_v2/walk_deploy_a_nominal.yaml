# Phase 9 condition A nominal: DR off (amendment 9). Exact deploy stack around Isaac Lab:
# the env's own soft limiter is OFF, the deploy ActuatorGate is the only limiter.
defaults:
  - walk_w1
action:
  rate_limit:
    enabled: false
domain_randomization:
  enabled: false
