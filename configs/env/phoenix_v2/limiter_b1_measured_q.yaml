# Phase B1: the deployed H25 policy in its own training distribution with the
# INCUMBENT limiter: target clipped to measured q +/- 0.175 rad (as trained).
defaults:
  - ../stand_v3_h25
action:
  rate_limit:
    enabled: true
    clip_mode: measured_q
    max_delta_per_step: 0.175
