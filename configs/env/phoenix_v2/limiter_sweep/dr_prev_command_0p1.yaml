# Phase B/C development sweep (dev seeds only). Condition: dr. Limiter: prev_command @ 0.1 rad/step.
defaults:
  - ../../stand_v3_h25
action:
  rate_limit:
    enabled: true
    clip_mode: prev_command
    max_delta_per_step: 0.1
