# Phase B/C development sweep (dev seeds only). Condition: nominal. Limiter: prev_command @ 0.175 rad/step.
defaults:
  - ../../stand_v3_h25
domain_randomization:
  enabled: false
action:
  rate_limit:
    enabled: true
    clip_mode: prev_command
    max_delta_per_step: 0.175
