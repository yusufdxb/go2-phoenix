# Phase B/C development sweep (dev seeds only). Condition: nominal. Limiter: hard_only @ 0 rad/step.
defaults:
  - ../../stand_v3_h25
domain_randomization:
  enabled: false
action:
  rate_limit:
    enabled: false
