# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/
pip-wheel-metadata/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Virtual envs
.venv/
venv/
/env/
ENV/

# Editors / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ML / data
checkpoints/**/*.pt
checkpoints/**/*.onnx
checkpoints/**/*.onnx.data
checkpoints/**/*.pth
!checkpoints/.gitkeep
data/**/*.parquet
data/**/*.mp4
data/**/*.npz
!data/.gitkeep

# TensorBoard / W&B
runs/
logs/
wandb/
outputs/
tb_logs/

# Isaac Sim / Omniverse scratch
*.usd.~
*.log

# Rendered media that should be regenerated
media/renders/
media/**/*.mp4

# Benchmarks (keep summaries, drop raw)
benchmarks/raw/

# Local config overrides
configs/local/
.env
.env.local

# Sweep-generated per-cell configs (regenerated by scripts/sweep_run.py)
configs/_generated/

# Lab session captures (synced to T7, not committed)
data/lab/

# Local scratchpads (not part of the public repo)
hardware prompt run*.txt

# Internal lab notes and planning: kept in the Obsidian vault, not the public repo
docs/sessions/
docs/superpowers/
docs/attachments/
docs/LAB_CARD_NEXT.md
docs/adapt_load_debug.md

# training artifacts / scratch
checkpoints/
FIXLIST.md

# reliability Phase-3 rollouts + logs (large / transient); results*/ IS tracked
reliability_eval/raw/
reliability_eval/logs/
reliability_eval/raw_stand/
reliability_eval/logs_stand/

# Exported policy binaries (regenerable from the checkpoint via export.py).
# The shield artifact itself IS tracked: it is small and it is the deliverable.
deploy/*.onnx
deploy/*.onnx.data
deploy/*.pt

# Closed-loop study raw arm outputs (regenerable); protocol/bundle/results tracked
reliability_eval/closed_loop/arm_*.npz
reliability_eval/closed_loop/*_pilot*
